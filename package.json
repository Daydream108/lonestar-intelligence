{
  "name": "lonestar-intelligence",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npx vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
{
  "devDependencies": {
    "@vitejs/plugin-react": "latest"
  },
  "scripts": {
    "dev": "vite",
    "build": "npx vite build",
    "preview": "vite preview"
  }
}
{
  "name": "lonestar-intelligence",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "npx vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest"
  },
  "dependencies": {
    "react": "18.x",
    "react-dom": "18.x"
  }
}
